#!/usr/bin/perl

while(<>){
	$l = $_;
	$l =~ s/(<italic>[^<>]*)(<;\/>)([^<>]*<\/italic>)/$1$3/g;
	$l =~ s/(<RES[^<>]*>[^<>]*)(<;\/>)([^<>]*<\/RES>)/$1$3/g;
	$l =~ s/(<ORG[^<>]*>[^<>]*)(<;\/>)([^<>]*<\/ORG>)/$1$3/g;
	$l =~ s/(\([^()]*)(<;\/>)([^()]*\))/$1$3/g;
	print $l;
}
